# Epic 3 Retrospective

## Metadata

- Project: CORD
- Epic: 3 - 关系查询、影响分析与数据导出
- Date: 2026-05-14
- Facilitator: Bob (Scrum Master)
- Mode: 基于工件的非互动回顾
- Evidence reviewed:
  - `_bmad-output/implementation-artifacts/sprint-status.yaml`
  - `_bmad-output/planning-artifacts/epics/epic-3关系查询影响分析与数据导出.md`
  - `_bmad-output/planning-artifacts/epics/epic-4关系管理与图谱修正.md`
  - `_bmad-output/implementation-artifacts/stories/3-1-queryservice-relation-query-one-hop-and-type-filter.md`
  - `_bmad-output/implementation-artifacts/stories/3-2-multi-hop-relation-traversal.md`
  - `_bmad-output/implementation-artifacts/stories/3-3-impactservice-change-impact-analysis.md`
  - `_bmad-output/implementation-artifacts/stories/3-4-json-snapshot-export.md`
  - `_bmad-output/implementation-artifacts/stories/3-5-statusservice-health-check.md`
  - `_bmad-output/implementation-artifacts/retrospectives/epic-2-retro-2026-05-09.md`
  - Latest Epic 3 code review summaries for Stories 3.1-3.5
  - `_bmad-output/implementation-artifacts/cr-rules/cr-todo-backlog.md`
  - `_bmad-output/project-context.md`
  - `_bmad-output/planning-artifacts/architecture/03-core-architectural-decisions.md`
  - `_bmad-output/planning-artifacts/architecture/04-implementation-patterns-consistency-rules.md`

## Epic Summary

Epic 3 的 5 个 Story 均已完成，交付了 CORD 的读侧能力闭环：一跳关系查询、多跳 BFS 遍历、变更影响分析、JSON 快照导出和图谱健康检查。

本 Epic 将 Epic 2 建立的文档关系图谱转化为可消费能力。用户现在可以通过 `cord query` 理解单文档关系，通过 `cord impact` 获取下游影响和建议动作，通过 `cord export` 生成适合 git 审阅的快照，通过 `cord status` 查看图谱健康与配置状态。

Sprint 状态中 Story 3.1-3.5 均为 `done`。本回顾完成后，`epic-3-retrospective` 标记为 `done`。回顾当日需要注意的是，`epic-3` 主状态仍为 `in-progress`；这与所有 Story 已完成的事实不一致，建议由流程 owner 单独确认是否更新主 Epic 状态。

## Post-Retrospective Polish Update

本节记录 2026-05-21 之后对 Epic 3 回顾结论的再校准。它不改写 2026-05-14 当天的回顾语境，只补充当前工件状态，方便后续 release readiness 直接消费。

- Sprint 状态当前已收口：`epic-3` 与 `epic-3-retrospective` 均为 `done`。
- CR TODO backlog 当前为 Open 0、Resolved 36；Epic 3 回顾中聚合的 TODO-024、TODO-026、TODO-027、TODO-028、TODO-029、TODO-030、TODO-031、TODO-032、TODO-033 均已 resolved。
- `query` / `impact` / `export` 默认只读路径副作用已治理，未初始化项目不再因只读命令创建 `.cord` 或空数据库。
- status 健康统计口径已冻结为 active + deprecated 全量关系快照；该裁决已同步到后续规则文档和测试。
- 导出可靠性、路径边界、配置错误路径副作用和排序稳定性均已进入后续治理并关闭。

## Team Participants

- Bob (Scrum Master): 回顾主持、流程连续性与 action item 收敛
- Alice (Product Owner): Epic 目标、用户价值与下一 Epic 依赖视角
- Charlie (Senior Dev): 查询语义、影响分析方向、导出与状态实现质量判断
- Dana (QA Engineer): 测试覆盖、CR 闭环与回归风险判断
- Winston (Architect): 架构规则、跨层边界与 Rule Document Registry 同步判断
- Fancyliu (Project Lead): 项目优先级与最终取舍 owner

## Facilitation Snapshot

Bob (Scrum Master): "Epic 3 已经把图谱从存储资产变成了可查询、可解释、可导出的产品能力。我们要看清楚哪些模式值得保留，也要诚实面对哪些边界还没完全冻结。"

Alice (Product Owner): "从产品角度看，这个 Epic 是 CORD 的价值显性化阶段。用户终于能看到关系、影响和健康状态，而不只是相信扫描已经写入数据库。"

Charlie (Senior Dev): "技术上最大的收获不是新增了几个命令，而是我们把遍历、过滤、方向、去重和只读副作用这些语义都测试化了。"

Dana (QA Engineer): "CR 发现的很多问题都是会真实误导用户的边界，不是形式问题。尤其是多跳过滤、影响分析回源和 status 只读副作用。"

Winston (Architect): "Epic 3 新增的 CR-QUERY 与 CR-STATUS 规则已经同步到规则文档。后续 Story 必须复用这些边界，而不是重新解释。"

## What Went Well

1. 读侧主链路完整交付。Story 3.1-3.5 覆盖查询、遍历、影响分析、导出、健康检查五个核心用户入口，并保持 CLI 薄壳 + Service 业务逻辑 + Repository 接口的架构边界。
2. QueryService 的语义在 CR 中显著变清晰。Story 3.2 修复了 `type` 过滤截断 BFS 路径的问题，并进一步修复了非匹配坏边在端点解析前阻断查询的问题；这直接沉淀为“可扩展边”和“可输出边”分离规则。
3. ImpactService 从通用查询复用转向自有传播语义。Story 3.3 通过 CR 修复了上游误报、低置信桥接边继续扩展、自环/回源环把源文档计入结果、重复命中膨胀 `totalCount` 等问题，最终形成有向三跳传播、阈值前置、按 impacted document 去重和 source self 排除的稳定语义。
4. ExportService 推动了配置契约补齐。Story 3.4 中 `project` 字段来源完成裁决，落地为 `cord.config.projectName` 优先、缺失回退项目根目录名，并同步更新 Rule Document Registry 三份文档。
5. StatusService 把“观测型命令必须只读”正式固化。Story 3.5 修复了未初始化项目执行 `cord status` 会创建 `.cord/cord.db` 的问题，并补充事务快照、dangling edge 统计和 cleanup best-effort 规则。
6. 测试纪律持续有效。每个 Story 都记录了窄测试、全量测试、lint、type-check 或 build 的通过证据；最终 Story 3.5 的 CR 摘要记录 `npm test`、`npm run lint`、`npm run build` 均通过。

## Challenges

1. 查询语义比初始 Story 描述更复杂。多跳查询不能简单把输出过滤当作遍历过滤；影响分析也不能简单复用通用双向查询再后过滤。这些都是通过 CR 才暴露的架构级细节。
2. 只读命令副作用治理在回顾当日不均衡。`status` 已修复为未初始化项目不创建存储，但 `query` / `impact` 默认 service 初始化仍由 TODO-028 跟踪；后续 TODO-028 已 resolved。
3. 性能验收在回顾当日仍存在测试稳健性问题。TODO-024 后续已 resolved，测试从易抖 wall-clock 比例迁移到更确定的 query planner / traversal 验证。
4. 导出快照的 git 审阅体验在回顾当日还有可靠性债。TODO-029、TODO-030、TODO-031、TODO-032 后续均已 resolved。
5. deprecated 关系的读侧语义在回顾当日尚未全局冻结。后续 TODO-033 已裁决：Status 健康统计按 active + deprecated 全量关系快照计算。
6. Sprint 状态口径在回顾当日继续出现漂移。Epic 3 的所有 Story 已完成，但 `epic-3` 主状态仍为 `in-progress`；当前 sprint 状态已修正为 done。

## Key Insights

1. 图查询功能的核心不是“能返回边”，而是“过滤、方向、深度、状态和坏数据处理都符合用户直觉且可解释”。
2. 对影响分析而言，关系边是传播路径，不只是展示对象。路径资格必须在扩展前判断，结果基数必须按受影响文档而不是关系命中数计算。
3. 观测型命令的价值来自可信边界：查看状态不能改变状态。Story 3.5 的修复应成为后续所有只读命令的模板。
4. JSON 快照是工程协作接口，不只是文件输出。排序稳定、覆盖语义、原子写入和路径边界会直接影响 git 审阅信任。
5. Rule Document Registry 在 Epic 3 中发挥了实际作用。新增 `projectName`、CR-QUERY、CR-STATUS 等规则都已同步到项目上下文和架构规则中，降低了后续 agent 重复踩坑的概率。

## Previous Retro Follow-Through

Epic 2 retrospective 提出的行动项在 Epic 3 中有如下跟进情况：

1. 确认并更新 Epic 2 主状态口径：完成。当前 `sprint-status.yaml` 中 `epic-2` 已为 `done`。
2. 将 Epic 2 CR TODO 按主题挂入后续计划：持续进行。CR TODO backlog 已继续维护，并新增 Epic 3 的 TODO-024、TODO-026 至 TODO-033 等项目。
3. 为 Epic 3 Story 明确“只消费图谱，不重新扫描”的边界：完成度高。Story 3.1-3.5 均围绕 Repository/Service 读侧契约实现，没有把扫描管线混入查询或导出主线。
4. 保留 Story 2.6 的 rename/move 保守匹配规则：完成。Epic 3 未引入生命周期重绑定变更，且 project context / 架构文档中仍保留 CR-SCAN-01 / D10 规则。
5. 在 Epic 3 查询和导出测试中加入来自 Epic 2 图谱的真实样本：部分完成。Story 3.2 和 3.3 已补 SQLite repository / CLI 集成路径；但从 Story 记录看，尚未明确证明 auto_scan 与 framework_preset 两类关系来源都被端到端覆盖。

结论：Epic 3 明显继承了 Epic 2 的“边界测试化 + CR TODO 资产化”习惯；仍需加强的是 Epic 主状态收尾和跨来源真实图谱样本覆盖。

## Technical Debt And Follow-up Backlog

本回顾不重复创建已存在 TODO，而是按后续治理主题聚合 Epic 3 新增和延续项：

1. 已关闭：查询与影响分析语义增强、只读命令副作用治理、查询性能测试稳健性、导出可靠性与 git 审阅体验、deprecated 关系健康统计口径等对应 TODO 均已 resolved。
2. 持续约束：后续新增读侧命令仍必须复用“输入校验先于默认 service / repository 初始化、只读观察不创建存储”的规则。
3. 当前重点：release 前继续以当前 JSON 快照 schema、只读命令契约和 status 全量统计口径为准，不回退到回顾当日的未冻结状态。

## Next Epic Preview

Epic 4 的目标是“关系管理与图谱修正”，包含手动添加/移除/标记 deprecated 关系、增量扫描保护手动修正、以及按文档类别配置更新策略。

Epic 4 对 Epic 3 的依赖非常直接：

1. Story 4.1 依赖 Story 3.1 输出 `relationId`，因为 remove / deprecate 需要可稳定定位关系。
2. Story 4.1 和 4.2 依赖 Query / Impact 对 `status='deprecated'` 的读侧语义：查询默认过滤但可 `includeDeprecated`，影响分析默认不沿 deprecated 关系传播。
3. Story 4.2 依赖 Epic 2 的增量扫描能力和 Epic 3 的状态/查询能力共同验证手动修正是否被保留。
4. Story 4.3 依赖配置 schema 与影响分析输出中的 `updateStrategy` 语义，需确保配置字段、默认策略和输出字段不漂移。
5. StatusService 的健康统计将与 Epic 4 的 deprecated 关系语义发生交汇，TODO-033 应在 Story 4.1 或 4.2 前后明确裁决。

Epic 4 当前方向无需重写。需要提前管理的是语义冻结：manual / framework_preset / auto_scan 的优先级、deprecated status 与 deprecated relationType 的差异、以及健康统计是否默认排除 deprecated 关系。

## Action Items

1. 更新 Epic 3 主状态口径。
   Owner: Scrum Master
   Status: 已完成。
   Completion gate: `sprint-status.yaml` 中 `epic-3` 与 Story 3.1-3.5 的完成状态一致，且不破坏现有注释结构。

2. 在 Epic 4 Story 4.1 / 4.2 中冻结 deprecated 读侧和健康统计语义。
   Owner: Product Owner + Architect
   Status: 已完成，TODO-033 resolved。
   Completion gate: 明确 Query、Impact、Export、Status 对 `status='deprecated'` 的各自口径，并同步到 Story AC / Dev Notes / 测试。

3. 将 `status` 的只读初始化模式提炼为 Query / Impact 后续治理规则。
   Owner: Architect + Senior Dev
   Status: 已完成，TODO-028 resolved。
   Completion gate: TODO-028 的后续处理方案明确区分“未初始化”与“已初始化但空图谱”，并保证输入错误或只读观察不产生存储副作用。

4. 为 Epic 4 准备关系来源优先级回归样本。
   Owner: QA Engineer + Dev
   Completion gate: 测试夹具同时覆盖 `manual`、`framework_preset`、`auto_scan`、`status='active'`、`status='deprecated'`，并覆盖冲突优先级。

5. 把 ExportService 的可靠性 TODO 保持为独立治理主题，不混入 Epic 4 主线。
   Owner: Product Owner + Senior Dev
   Status: 已完成，TODO-029 至 TODO-032 resolved。
   Completion gate: TODO-029 至 TODO-032 有明确后续处理位置或保留理由。

## Preparation Tasks For Epic 4

1. 复核 `RelationEdge.status`、`relationType: deprecated` 和 `status='deprecated'` 的差异，避免 Story 4.1 把两者混用。
   Owner: Architect

2. 明确手动关系的唯一性维度是否继续包含 `source`，并确保与 Repository 唯一索引和 CR-REPO-04 保持一致。
   Owner: Senior Dev

3. 为 `RelationService` 设计错误码、suggestion 和 JSON 错误载荷测试，延续 Epic 3 的 NFR19 做法。
   Owner: Dev + QA

4. 在增量扫描保护测试中复用 Epic 2 的生命周期检测样本，同时加入 Epic 3 的 query / impact 验证，证明手动修正不仅写入成功，也被读侧正确消费。
   Owner: QA Engineer

5. 为 `updateStrategies` 明确默认值、未知 key 宽容处理和影响分析输出字段契约，避免 Story 4.3 与 Story 3.3 的输出语义漂移。
   Owner: Product Owner + Architect

## Critical Path

1. Story 4.1 启动前确认 relation identity：`relationId` 是管理关系的主句柄，重复关系判断必须与 Repository 唯一性约束一致。
2. Story 4.1 / 4.2 启动前确认 deprecated status 的跨命令语义，不让 Query、Impact、Export、Status 输出互相矛盾。
3. Story 4.2 启动前确认手动修正优先级规则：manual > framework_preset > auto_scan，并准备冲突回归测试。
4. Story 4.3 启动前确认 `updateStrategies` 的配置 schema、默认策略和 impact 输出字段。
5. Epic 4 执行期间继续维护 CR TODO backlog，避免非阻塞项消失在审查摘要中。

## Significant Discovery Check

本次 retrospective 未发现需要改写 Epic 4 规划的重大认知偏差。Epic 3 的输出能力已经为 Epic 4 的关系管理提供了足够读侧基础。

需要带入 Epic 4 的不是方向变更，而是语义边界：deprecated status、关系来源优先级、只读命令副作用和健康统计口径必须在对应 Story 中显式冻结。

Epic 4 replanning required: no.

## Readiness Assessment

- Testing and quality: 强。Epic 3 每个 Story 都有窄测试和全量回归记录，CR 阻塞项均闭环。
- Deployment and release: 未发现新的发布阻塞；既有 release hardening TODO 后续已 resolved。
- Stakeholder acceptance: 从 Story / CR 工件角度看已具备内部接受条件。
- Technical health: 主链路稳定；读侧副作用治理和 deprecated 健康统计口径后续已收口。
- Unresolved blockers: 无阻塞 Epic 4 启动的硬问题。

## Commitments And Next Steps

1. 用 Epic 3 沉淀的遍历语义和只读命令规则约束 Epic 4 的关系管理实现。
2. 在 Epic 4 中优先冻结 deprecated / manual / source priority 语义，而不是边实现边补裁决。
3. 继续把 CR TODO backlog 当作真实工程资产维护；当前 Open 0 的状态需要通过后续登记和关闭纪律保持。
4. 对 sprint status 的 Epic 主状态收尾建立更明确的流程，避免连续 Epic 出现 Story 完成但 Epic 状态未同步的漂移。

## Retrospective Outcome

- Epic review result: complete
- Retrospective result: complete
- Epic 4 replanning required: no
- Recommended next move: 保持 Epic 3 读侧契约、只读命令规则和导出可靠性基线，进入 release-readiness 时复核这些边界仍由测试覆盖
