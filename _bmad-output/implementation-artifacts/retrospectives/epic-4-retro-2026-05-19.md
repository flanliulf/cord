# Epic 4 Retrospective

## Metadata

- Project: CORD
- Epic: 4 - 关系管理与图谱修正
- Date: 2026-05-19
- Facilitator: Bob (Scrum Master)
- Mode: 基于工件的非互动回顾
- Evidence reviewed:
  - `_bmad-output/implementation-artifacts/sprint-status.yaml`
  - `_bmad-output/planning-artifacts/epics/epic-4关系管理与图谱修正.md`
  - `_bmad-output/planning-artifacts/epics/epic-5ai-ide-集成mcp-server-hooks-指令注入.md`
  - `_bmad-output/implementation-artifacts/stories/4-1-relationservice-manual-add-and-remove-relations.md`
  - `_bmad-output/implementation-artifacts/stories/4-2-convergence-protection-and-source-priority.md`
  - `_bmad-output/implementation-artifacts/stories/4-3-document-category-update-strategy-config.md`
  - `_bmad-output/implementation-artifacts/code-reviews/4-1-code-review/4-1-code-review-evaluation-20260515-round-3.md`
  - `_bmad-output/implementation-artifacts/code-reviews/4-2-code-review/4-2-code-review-evaluation-20260515-round-2.md`
  - `_bmad-output/implementation-artifacts/code-reviews/4-3-code-review/4-3-code-review-evaluation-20260515-round-2.md`
  - `_bmad-output/implementation-artifacts/retrospectives/epic-3-retro-2026-05-14.md`
  - `_bmad-output/implementation-artifacts/cr-rules/cr-todo-backlog.md`
  - `_bmad-output/project-context.md`
  - `_bmad-output/planning-artifacts/architecture/03-core-architectural-decisions.md`
  - `_bmad-output/planning-artifacts/architecture/04-implementation-patterns-consistency-rules.md`

## Epic Summary

Epic 4 的 3 个 Story 均已完成，交付了 CORD 的关系修正闭环：用户可以手动添加、硬删除或标记关系为 deprecated；增量扫描会保护手动修正并按来源优先级收敛；影响分析结果可以按文档类别返回 `updateStrategy`，为后续同步编排提供机器可读元数据。

本 Epic 把 Epic 3 的读侧图谱能力推进到可修正、可保护、可逐步收敛的阶段。它直接支撑 Epic 5 的 MCP 关系管理工具，让 AI IDE 可以通过原子化 Tool 调用执行用户的自然语言关系修正意图。

Sprint 状态中 Story 4.1-4.3 均为 `done`。本回顾完成后，`epic-4-retrospective` 标记为 `done`。需要注意的是，`epic-4` 主状态当前仍为 `in-progress`；这与所有 Story 已完成的事实不一致，建议由流程 owner 单独确认是否更新主 Epic 状态。

## Team Participants

- Bob (Scrum Master): 回顾主持、流程连续性与 action item 收敛
- Alice (Product Owner): 用户修正闭环、收敛价值与下一 Epic 依赖视角
- Charlie (Senior Dev): RelationService、ScanService 优先级与迁移风险判断
- Dana (QA Engineer): CR 闭环、测试覆盖与回归风险判断
- Winston (Architect): Rule Document Registry、配置契约与跨命令语义判断
- Fancyliu (Project Lead): 项目优先级与最终取舍 owner

## Facilitation Snapshot

Bob (Scrum Master): "Epic 4 的主题很清楚：让图谱不只是被扫描出来，还能被用户修正，并且越用越准。"

Alice (Product Owner): "这正好对应 PRD 里的异常恢复旅程。用户发现误判或遗漏后，可以直接让 Agent 修正关系，而不是忍受错误结果。"

Charlie (Senior Dev): "技术上最关键的是把 deprecated 从 relationType 里拆出来，变成独立 status，再让 manual 来源成为保护边界。否则收敛保护会很脆。"

Dana (QA Engineer): "CR 这次抓到的不是表面问题。比如 4.2 第一轮发现 deprecated 只保护 manual 来源，真实用户路径会漏保护，这类问题如果进到 Epic 5 会直接影响 MCP 工具可信度。"

Winston (Architect): "Rule Document Registry 在 4.3 再次发挥作用。`updateStrategies` 的 schema、默认回退、配置模板契约必须三份文档同步，否则后续 init 和 impact 会各说各话。"

## What Went Well

1. 关系管理语义完成闭环。Story 4.1 明确 `removeRelation` 是硬删除，`deprecateRelation` 使用 `status='deprecated'` 且保留原 `relationType`，手动添加固定写入 `source='manual'`、`status='active'`。
2. 迁移加固从单点修复上升为可复用规则。Story 4.1 补充 `relations.status` 增量迁移，并在 CR 后修复“列已存在但索引缺失”的部分迁移数据库场景，强化了 D2 / CR-REPO-06 的迁移子步骤独立幂等要求。
3. 收敛保护覆盖了真实用户路径。Story 4.2 修复后，用户手动 deprecated 原本由 `auto_scan` 或 `framework_preset` 发现的关系时，会转为 `source='manual'`，从而进入增量扫描保护集合。
4. 来源优先级在两个阶段都被测试化。Story 4.2 不只在持久化写入阶段处理 `manual > framework_preset > auto_scan`，也修复了同批次去重阶段按 confidence 误选 auto_scan 的问题。
5. rebuild 的数据保护边界清晰。`cord scan --rebuild` 在存在 manual 关系时会警告并确认；`--force` 可跳过确认但仍提示 manual 关系删除数量，符合 v0.1 对“全量重建一致性不包含 manual 状态”的裁决。
6. `updateStrategies` 与影响分析输出对齐。Story 4.3 新增 `auto | suggest | log_only`，默认回退 `suggest`，ImpactService 和 `impact` CLI 都输出 `updateStrategy`，同时同步 Rule Document Registry 三份文档。
7. CR 闭环质量高。Story 4.1 经过 3 轮、Story 4.2 和 4.3 经过 2 轮后均无剩余阻塞项或新增 CR TODO；每个 Story 都记录了定向测试、全量测试、lint 或 type-check 证据。

## Challenges

1. deprecated 语义容易被混用。Epic 4 一开始必须反复区分 `relationType` 与 `status='deprecated'`；如果把 deprecated 作为关系类型处理，扫描恢复、查询过滤和影响分析传播都会出现语义漂移。
2. ScanService 的保护逻辑存在多入口风险。source 文档删边、写入冲突、同批次去重、rebuild 删除都可能影响 manual 关系。最终方案覆盖了这些入口，但复杂度明显高于“扫描时保留 manual 边”这一句话。
3. 规则文档同步仍是高风险点。Story 4.3 第一轮 CR 发现 `updateStrategies` 旧语义残留，说明 Rule Document Registry 可以降低漂移，但仍需要审查流程持续约束。
4. `cord init` 模板契约在 Story 4.3 时只能文档化。Story 4.3 完成时 runtime `cord init` 尚未实现，因此 `updateStrategies` 示例块通过规则文档承接，后续实现必须再次验证模板输出没有漏掉该字段。
5. status 健康统计口径仍未冻结。TODO-033 仍为 open：`StatusService` 的关系总数和按类型分布是否排除 deprecated 关系，还没有产品/Story 裁决。
6. Epic 主状态收尾继续漂移。Epic 3 回顾已指出同类问题；当前 Epic 4 的所有 Story 已 done，但 `epic-4` 主状态仍是 `in-progress`。

## Key Insights

1. “用户修正”不是简单 CRUD，而是图谱收敛机制。手动修正必须影响后续扫描、查询、影响分析和 Agent 工具调用的行为。
2. deprecated 的正确建模是状态位，不是关系类型。这样才能让原始关系语义保留，同时让读侧和写侧按状态做一致决策。
3. 优先级规则必须覆盖扫描流水线中的每个候选淘汰点。只在最终写库时处理冲突，仍可能让高优先级关系在前置 dedupe 中被丢弃。
4. destructive 操作需要用户可见的保护边界。`--rebuild` 删除 manual 关系是合理的 v0.1 裁决，但必须在 CLI 层显式告知和确认。
5. 策略元数据与人读建议应并存。`updateStrategy` 服务机器编排，`suggestedAction` 服务人类理解，两者不能互相替代。
6. 文档规则同步本身需要测试和审查。规则文档不是“说明书”，而是后续 Agent 和 Story 的输入契约。

## Previous Retro Follow-Through

Epic 3 retrospective 提出的行动项在 Epic 4 中有如下跟进情况：

1. 更新 Epic 3 主状态口径：完成。当前 `sprint-status.yaml` 中 `epic-3` 已为 `done`。
2. 在 Epic 4 Story 4.1 / 4.2 中冻结 deprecated 读侧和健康统计语义：部分完成。`status='deprecated'` 与 `relationType` 的差异、manual deprecated 保护、Query / Impact 默认过滤语义已经落地；但 Status 健康统计是否排除 deprecated 关系仍由 TODO-033 跟踪。
3. 将 `status` 的只读初始化模式提炼为 Query / Impact 后续治理规则：未在 Epic 4 主线处理。TODO-028 仍应保持为独立治理主题。
4. 为 Epic 4 准备关系来源优先级回归样本：完成。Story 4.2 测试覆盖 manual、framework_preset、auto_scan、active / deprecated 以及同批次冲突优先级。
5. 把 ExportService 的可靠性 TODO 保持为独立治理主题：完成。Epic 4 未把 TODO-029 至 TODO-032 混入关系管理主线。

结论：Epic 4 很好地继承了 Epic 3 的“语义先冻结、边界测试化、CR TODO 资产化”习惯；仍需补齐的是 status 健康统计裁决和 Epic 主状态收尾流程。

## Technical Debt And Follow-up Backlog

1. TODO-033 仍需裁决：status 健康统计是否只计 active 关系，或同时报告 active / deprecated 分布。
2. TODO-007 仍需保留：Story 4.1 加固了部分迁移数据库场景，但旧版 v1 基线整体升级债务仍需在稳定发布前处理。
3. TODO-010 仍需保留：Story 4.1 补入部分迁移场景，但迁移版本记录、失败回滚、WAL 模式的直接断言仍未被取代。
4. Story 4.3 已知限制需持续显式化：rename / move 跨类别后，`docType` 不重算会导致策略可能沿用旧类别；当前缓解方式是 `--rebuild` 后重新计算。
5. manual 关系备份 / 回放能力未纳入 v0.1。`--rebuild` 会删除 manual 关系并提示用户，未来如需更强保护，应设计导出与恢复路径。

## Next Epic Preview

Epic 5 的目标是“AI IDE 集成（MCP Server + Hooks + 指令注入）”，包含 MCP Server 核心工具、关系管理 Tools、IDE 适配器、`cord init` 和 Hooks / Skills 生成。

Epic 5 对 Epic 4 的依赖非常直接：

1. Story 5.2 的 `add_relation`、`remove_relation`、`deprecate_relation` 必须复用 Story 4.1 的 RelationService 语义。
2. MCP 关系管理 Tools 必须保持 `add_relation` 使用业务三元组、`remove_relation` / `deprecate_relation` 使用 `relationId` 的签名边界。
3. `deprecate_relation` 的输出和后续扫描行为必须保留 `source='manual'`、`status='deprecated'` 的保护语义。
4. Story 5.4 的 `cord init` 配置模板必须包含 Story 4.3 裁决出的 `updateStrategies` 示例块，避免 init 生成物与配置规则文档漂移。
5. Hooks / Skills 的同步建议应消费 `updateStrategy`，但 v0.1 不应越界实现自动编排执行。

历史状态说明：当前 `sprint-status.yaml` 已显示 Epic 5 的 Story 5.1-5.5 均为 `done`。因此本回顾记录的是 Epic 4 对 Epic 5 的规划依赖和已知交接点，而不是重新启动 Epic 5。

## Action Items

1. 确认 Epic 4 主状态口径。
   Owner: Scrum Master
   Completion gate: `sprint-status.yaml` 中 `epic-4` 与 Story 4.1-4.3 的完成状态一致，且不破坏现有注释结构。

2. 裁决 TODO-033：status 健康统计口径。
   Owner: Product Owner + Architect
   Completion gate: 明确 `StatusService` 对 active / deprecated 关系的统计规则，并同步 Story / 测试 / Rule Document Registry 中受影响内容。

3. 复核 Story 5.4 的 init 配置模板是否承接 `updateStrategies`。
   Owner: Architect + QA Engineer
   Completion gate: `cord init` 生成的 YAML / JSON 示例与 D6、P17、Story 4.3 的配置契约一致。

4. 保持 manual 关系备份 / 回放为独立后续主题。
   Owner: Product Owner + Senior Dev
   Completion gate: 明确这是 v0.1 之外的增强，或为后续版本建立独立 Story；不要混入现有 rebuild 保护逻辑。

5. 建立 Epic 主状态收尾检查。
   Owner: Scrum Master
   Completion gate: retrospective finalizer 或手工收尾清单中包含“所有 Story done 后确认 epic 主状态”的检查项。

## Preparation Tasks For Epic 5

1. 验证 MCP 关系管理 Tools 的 schema 与 RelationService 输入输出保持一致。
   Owner: Dev + QA

2. 验证 MCP 关系管理 Tools 不削弱已有 4 个核心 Tool 的 schema 稳定性。
   Owner: QA Engineer

3. 验证 `deprecate_relation` 端到端路径会触发 manual deprecated 保护语义。
   Owner: Dev + QA

4. 验证 `cord init` 生成配置文件包含 `updateStrategies` 示例块。
   Owner: Architect + QA Engineer

5. 验证 Hooks / Skills 对 `updateStrategy` 的使用仍停留在建议与编排元数据层，不自动执行超出 v0.1 边界的同步。
   Owner: Product Owner + Architect

## Critical Path

1. MCP 关系管理 Tools 必须以 RelationService 为唯一业务 owner，入口层只做 schema 校验、调用和输出格式化。
2. `deprecate_relation` 必须保留 Story 4.2 修复后的真实保护路径：原自动来源关系被用户 deprecated 后转为 manual deprecated。
3. `cord init` 的配置模板必须承接 `updateStrategies`，否则 Story 4.3 的配置能力会在首次体验中隐性缺失。
4. Hooks / Skills 只能消费策略元数据，不应把 Story 4.3 明确排除的自动编排执行提前塞入 v0.1。
5. status 健康统计裁决不能被某个代码改动顺手决定；必须先有产品/架构裁决，再同步实现和测试。

## Significant Discovery Check

本次 retrospective 未发现需要改写 Epic 5 总方向的重大认知偏差。Epic 4 已经为 Epic 5 的 AI IDE 关系修正能力提供了稳定 Service 基础。

需要带入 Epic 5 的重点不是方向变更，而是契约保持：MCP Tool 必须复用 RelationService 语义，`cord init` 必须承接 `updateStrategies` 配置模板，Hooks / Skills 必须尊重 v0.1 的“返回策略、不自动执行编排”边界。

Epic 5 replanning required: no.

## Readiness Assessment

- Testing and quality: 强。三个 Story 均有定向测试和回归记录，CR 阻塞项均闭环。
- Release readiness: 未发现新的发布阻塞；既有 release hardening 和迁移基线 TODO 仍保持 open。
- Stakeholder acceptance: 从 Story / CR 工件角度看已具备内部接受条件。
- Technical health: 关系管理主链路稳定；status 健康统计口径和 manual 关系备份能力仍需独立治理。
- Unresolved blockers: 无阻塞 Epic 5 方向的硬问题。

## Commitments And Next Steps

1. 把 Epic 4 的 RelationService 语义作为 Epic 5 MCP 写操作的唯一业务契约。
2. 在后续收尾中确认 Epic 4 主状态，避免 Story 完成但 Epic 状态滞留。
3. 对 TODO-033 做显式产品/架构裁决，不让 status 健康统计口径随代码实现漂移。
4. 复核 `cord init` 的配置模板，确保 Story 4.3 的 `updateStrategies` 能进入用户首次初始化体验。
5. 继续把 CR TODO backlog 当作真实工程资产维护，尤其是迁移基线、只读副作用、导出可靠性和 status 统计口径。

## Retrospective Outcome

- Epic review result: complete
- Retrospective result: complete
- Epic 5 replanning required: no
- Recommended next move: 完成 Epic 4 主状态收尾，并对 TODO-033 与 init 模板承接做专项确认
